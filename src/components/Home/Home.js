import React from 'react';
import './Home.css';

const Home = () => {
    return (
        <div className="home">
            <h1>Welcome to Agent M2!</h1>
            <p>
                My focus is to not only make you a better Mathematician but also a better problem
                solver.
            </p>
            <h3>So, Let's get going!</h3>

            <small>Select a subject from the navigation bar...</small>
        </div>
    );
};

export default Home;